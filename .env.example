# 127.0.0.1 rather than localhost: the container publishes on IPv4, and
# resolving localhost to ::1 first makes connections hang.
DATABASE_URL=postgres://badd:badd@127.0.0.1:5433/badd_boys

# The shared code the group types in once. Change it to whatever you like.
GROUP_CODE=change-me
